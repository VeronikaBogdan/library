import styled from 'styled-components/native';

import { BLACK40 } from '../../styles/constant';

export const StyledRating = styled.View`
  flex-direction: row;
`;

export const StyledText = styled.Text`
  font-weight: 400;
  font-size: 20px;
  line-height: 24px;
  color: ${BLACK40};
`;
